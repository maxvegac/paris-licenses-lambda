# Environment Configuration
NODE_ENV=development

# AWS Configuration (for local development)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Application Configuration
PORT=3000

# Paris API Configuration
PARIS_API_EMAIL=your_email@example.com
PARIS_API_PASSWORD=your_password_here

# SMTP Configuration (Gmail)
SMTP_USER=your_gmail@gmail.com
SMTP_PASSWORD=your_app_password_here
SMTP_FROM_EMAIL=noreply@tudominio.com
SMTP_FROM_NAME=Equipo IVI

# Development Email Settings
# Set to 'true' to redirect all emails to DEV_EMAIL in development
DEV_EMAIL_REDIRECT=false
# Email address to receive redirected emails in development (defaults to SMTP_USER if not set)
DEV_EMAIL=your_dev_email@gmail.com

# Facto API Configuration
# Set to 'true' to use demo/test credentials instead of production
FACTO_TEST_MODE=true
# Production credentials (only used when FACTO_TEST_MODE=false)
FACTO_USER=76373632-6/786c
FACTO_PASS=d7364e0b3edcfce2547dee444850cf3d

# Invoice Storage Configuration
# S3 bucket for storing electronic invoices (boletas)
INVOICES_BUCKET=paris-licenses-invoices-xxxxxxxx
# DynamoDB table for tracking invoices
INVOICES_TABLE_NAME=paris-licenses-invoices

